let globals: any = new.env(parent = emptyenv(), hash = TRUE)

type Config: list {
  types: char,
  yields: char 
}

func write_config() {
  let config: Config = Config(
    types = globals$types,
    yields = globals$yields
  )

  print(config)
}
